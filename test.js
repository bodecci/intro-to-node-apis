//when we have a server, there is no browser 
//on the server, node.js runs on the terminal
//there is no window on the server, no DOM on the server
// you run the code in terminal by, node test.js
//There is no DOM or window in node.js

let a = 10;
let firstName = 'Chris';

console.log(firstName);
console.log(a*5);


function add(x, y) {
    return (x +y);
}

console.log(add(3,4));
